# React Frontend Example

This is a minimal React frontend skeleton demonstrating how to call a backend AI endpoint.

Files:
- `App.jsx` — simple UI for input and showing responses.

Run:

1. Ensure a backend example is running (see `../node-backend`).
2. Start the frontend with your preferred toolchain (e.g., `npm start`).
