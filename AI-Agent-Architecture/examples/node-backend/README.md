# Node Backend Example

This is a minimal Node.js Express backend that proxies requests to an AI model.

Files:
- `server.js` — example Express server exposing `/api/ai`.

Run:

1. Install dependencies: `npm install express node-fetch`.
2. Start: `node server.js`.
