# Next.js Edge Example

This folder shows where to place server/edge API routes in Next.js to run lightweight agent logic near the UI.

Look for `app/api/ai/route.js` or `pages/api/ai.js` in a real Next.js app.
