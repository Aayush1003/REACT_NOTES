# React Skeleton for Exercises

This is a minimal React app to use as a starting point for exercises and capstone work.

Run:

1. `npm init -y`
2. `npm install react react-dom` (or use Create React App / Vite for a full dev setup)

Files:
- `App.jsx` — small demo component
